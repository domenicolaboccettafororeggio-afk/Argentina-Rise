decision_category_energy = {
	ARG_rise_develop_fossil_fuels = {
		icon = generic_industry

		allowed = {
			original_tag = ARG
		}

		visible = {
			original_tag = ARG
		}

		available = {
			original_tag = ARG
			has_tech = excavation3
			check_variable = { treasury > 49.99 }
			any_owned_state = {
				is_core_of = ARG
				is_in_home_area = yes
				NOT = { has_state_category = state_inhospitable }
				NOT = { has_state_category = state_military_base }
			}
		}

		cost = 300
		fire_only_once = no
		days_remove = 30
		days_re_enable = 30

		modifier = {
			civilian_factory_use = 10
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_rise_develop_fossil_fuels"

			set_temp_variable = { treasury_change = -50.00 }
			modify_treasury_effect = yes
		}

		remove_effect = {
			random_owned_controlled_state = {
				limit = {
					is_core_of = ARG
					is_in_home_area = yes
					NOT = { has_state_category = state_inhospitable }
					NOT = { has_state_category = state_military_base }
				}

				add_resource = {
					type = oil
					amount = 50
				}
			}

			ingame_update_setup = yes
		}

		ai_will_do = {
			factor = 0
		}
	}

	ARG_rise_develop_light_metals = {
		icon = generic_industry

		allowed = {
			original_tag = ARG
		}

		visible = {
			original_tag = ARG
		}

		available = {
			original_tag = ARG
			has_tech = excavation3
			check_variable = { treasury > 49.99 }
			any_owned_state = {
				is_core_of = ARG
				is_in_home_area = yes
				NOT = { has_state_category = state_inhospitable }
				NOT = { has_state_category = state_military_base }
			}
		}

		cost = 300
		fire_only_once = no
		days_remove = 30
		days_re_enable = 30

		modifier = {
			civilian_factory_use = 10
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_rise_develop_light_metals"

			set_temp_variable = { treasury_change = -50.00 }
			modify_treasury_effect = yes
		}

		remove_effect = {
			random_owned_controlled_state = {
				limit = {
					is_core_of = ARG
					is_in_home_area = yes
					NOT = { has_state_category = state_inhospitable }
					NOT = { has_state_category = state_military_base }
				}

				add_resource = {
					type = aluminium
					amount = 40
				}
			}

			ingame_update_setup = yes
		}

		ai_will_do = {
			factor = 0
		}
	}

	ARG_rise_develop_technology_metals = {
		icon = generic_industry

		allowed = {
			original_tag = ARG
		}

		visible = {
			original_tag = ARG
		}

		available = {
			original_tag = ARG
			has_tech = excavation3
			check_variable = { treasury > 49.99 }
			any_owned_state = {
				is_core_of = ARG
				is_in_home_area = yes
				NOT = { has_state_category = state_inhospitable }
				NOT = { has_state_category = state_military_base }
			}
		}

		cost = 300
		fire_only_once = no
		days_remove = 30
		days_re_enable = 30

		modifier = {
			civilian_factory_use = 10
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_rise_develop_technology_metals"

			set_temp_variable = { treasury_change = -50.00 }
			modify_treasury_effect = yes
		}

		remove_effect = {
			random_owned_controlled_state = {
				limit = {
					is_core_of = ARG
					is_in_home_area = yes
					NOT = { has_state_category = state_inhospitable }
					NOT = { has_state_category = state_military_base }
				}

				add_resource = {
					type = tungsten
					amount = 25
				}
			}

			ingame_update_setup = yes
		}

		ai_will_do = {
			factor = 0
		}
	}

	ARG_rise_develop_precious_metals = {
		icon = generic_industry

		allowed = {
			original_tag = ARG
		}

		visible = {
			original_tag = ARG
		}

		available = {
			original_tag = ARG
			has_tech = excavation3
			check_variable = { treasury > 49.99 }
			any_owned_state = {
				is_core_of = ARG
				is_in_home_area = yes
				NOT = { has_state_category = state_inhospitable }
				NOT = { has_state_category = state_military_base }
			}
		}

		cost = 300
		fire_only_once = no
		days_remove = 30
		days_re_enable = 30

		modifier = {
			civilian_factory_use = 10
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_rise_develop_precious_metals"

			set_temp_variable = { treasury_change = -50.00 }
			modify_treasury_effect = yes
		}

		remove_effect = {
			random_owned_controlled_state = {
				limit = {
					is_core_of = ARG
					is_in_home_area = yes
					NOT = { has_state_category = state_inhospitable }
					NOT = { has_state_category = state_military_base }
				}

				add_resource = {
					type = chromium
					amount = 20
				}
			}

			ingame_update_setup = yes
		}

		ai_will_do = {
			factor = 0
		}
	}

	ARG_rise_develop_steel = {
		icon = generic_industry

		allowed = {
			original_tag = ARG
		}

		visible = {
			original_tag = ARG
		}

		available = {
			original_tag = ARG
			has_tech = excavation3
			check_variable = { treasury > 49.99 }
			any_owned_state = {
				is_core_of = ARG
				is_in_home_area = yes
				NOT = { has_state_category = state_inhospitable }
				NOT = { has_state_category = state_military_base }
			}
		}

		cost = 300
		fire_only_once = no
		days_remove = 30
		days_re_enable = 30

		modifier = {
			civilian_factory_use = 10
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_rise_develop_steel"

			set_temp_variable = { treasury_change = -50.00 }
			modify_treasury_effect = yes
		}

		remove_effect = {
			random_owned_controlled_state = {
				limit = {
					is_core_of = ARG
					is_in_home_area = yes
					NOT = { has_state_category = state_inhospitable }
					NOT = { has_state_category = state_military_base }
				}

				add_resource = {
					type = steel
					amount = 60
				}
			}

			ingame_update_setup = yes
		}

		ai_will_do = {
			factor = 0
		}
	}

	ARG_rise_develop_rubber = {
		icon = generic_industry

		allowed = {
			original_tag = ARG
		}

		visible = {
			original_tag = ARG
		}

		available = {
			original_tag = ARG
			has_tech = excavation3
			check_variable = { treasury > 49.99 }
			any_owned_state = {
				is_core_of = ARG
				is_in_home_area = yes
				NOT = { has_state_category = state_inhospitable }
				NOT = { has_state_category = state_military_base }
			}
		}

		cost = 300
		fire_only_once = no
		days_remove = 30
		days_re_enable = 30

		modifier = {
			civilian_factory_use = 10
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_rise_develop_rubber"

			set_temp_variable = { treasury_change = -50.00 }
			modify_treasury_effect = yes
		}

		remove_effect = {
			random_owned_controlled_state = {
				limit = {
					is_core_of = ARG
					is_in_home_area = yes
					NOT = { has_state_category = state_inhospitable }
					NOT = { has_state_category = state_military_base }
				}

				add_resource = {
					type = rubber
					amount = 35
				}
			}

			ingame_update_setup = yes
		}

		ai_will_do = {
			factor = 0
		}
	}
}