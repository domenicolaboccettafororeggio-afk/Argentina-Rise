decision_category_energy = {
	ARG_rise_build_renewable_energy_industry = {
		icon = GFX_decision_energy_build_button

		allowed = {
			original_tag = ARG
		}

		visible = {
			original_tag = ARG
		}

		available = {
			original_tag = ARG
			check_variable = { treasury > 5.99 }
			any_owned_state = {
				is_core_of = ARG
				is_in_home_area = yes
				free_building_slots = {
					building = synthetic_refinery
					size > 0
					include_locked = yes
				}
			}
		}

		cost = 50
		fire_only_once = no
		days_remove = 30
		days_re_enable = 30

		modifier = {
			civilian_factory_use = 5
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_rise_build_renewable_energy_industry"
			set_temp_variable = { treasury_change = -6.00 }
			modify_treasury_effect = yes
		}

		remove_effect = {
			random_owned_controlled_state = {
				limit = {
					is_core_of = ARG
					is_in_home_area = yes
					free_building_slots = {
						building = synthetic_refinery
						size > 0
						include_locked = yes
					}
				}
				add_building_construction = {
					type = synthetic_refinery
					level = 1
					instant_build = yes
				}
			}
			ingame_update_setup = yes
		}

		ai_will_do = {
			factor = 0
		}
	}

	ARG_rise_build_nuclear_power_plant = {
		icon = GFX_decision_energy_build_button

		allowed = {
			original_tag = ARG
		}

		visible = {
			original_tag = ARG
		}

		available = {
			original_tag = ARG
			check_variable = { treasury > 39.99 }
			any_owned_state = {
				is_core_of = ARG
				is_in_home_area = yes
				free_building_slots = {
					building = nuclear_reactor
					size > 0
					include_locked = yes
				}
			}
		}

		cost = 200
		fire_only_once = no
		days_remove = 60
		days_re_enable = 30

		modifier = {
			civilian_factory_use = 10
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_rise_build_nuclear_power_plant"
			set_temp_variable = { treasury_change = -40.00 }
			modify_treasury_effect = yes
		}

		remove_effect = {
			random_owned_controlled_state = {
				limit = {
					is_core_of = ARG
					is_in_home_area = yes
					free_building_slots = {
						building = nuclear_reactor
						size > 0
						include_locked = yes
					}
				}
				add_building_construction = {
					type = nuclear_reactor
					level = 1
					instant_build = yes
				}
			}
			ingame_update_setup = yes
		}

		ai_will_do = {
			factor = 0
		}
	}

	ARG_rise_build_grand_hydroelectric_dam = {
		icon = GFX_decision_energy_build_button

		allowed = {
			original_tag = ARG
		}

		visible = {
			original_tag = ARG
		}

		available = {
			original_tag = ARG
			check_variable = { treasury > 59.99 }
			any_owned_state = {
				is_core_of = ARG
				is_in_home_area = yes
			}
		}

		cost = 250
		fire_only_once = no
		days_remove = 120
		days_re_enable = 30

		modifier = {
			civilian_factory_use = 15
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_rise_build_grand_hydroelectric_dam"
			set_temp_variable = { treasury_change = -60.00 }
			modify_treasury_effect = yes
		}

		remove_effect = {
			random_owned_controlled_state = {
				limit = {
					is_core_of = ARG
					is_in_home_area = yes
				}
				set_temp_variable = { electric_addition = 15 }
				set_temp_variable = { storage_addition = 0 }
				add_hydroelectric_energy_production_effect = yes
				if = {
					limit = {
						free_building_slots = {
							building = energy_infrastructure
							size > 0
							include_locked = yes
						}
					}
					add_building_construction = {
						type = energy_infrastructure
						level = 1
						instant_build = yes
					}
				}
			}
			ingame_update_setup = yes
		}

		ai_will_do = {
			factor = 0
		}
	}
}